# Local ML stack (torch installed separately as a CPU-only wheel — see README)
sentence-transformers>=3.0,<6
chromadb>=0.5

# FastAPI web framework + ASGI server
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9        # for file upload support

# LLM backend client
openai>=1.40

# Structured-output validation
jsonschema>=4.20

# PDF extraction (optional for UI upload)
pypdf>=3.10

# MCP (Model Context Protocol) client/server
mcp>=1.0,<2

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27                    # for FastAPI TestClient
